#服务应用名称
spring:
  application:
    name: user-consumer
    desc: 用户中心调用者
  cloud:
    config:
      #配置中心地址
      uri: http://127.0.0.1:7777/
      #环境
      profile: dev
      #分支
      label: master

#服务端口号
server:
  port: 9001

